## 6. CO2 loss by DOC & POC loss

CO2_loss_DPOC0 <- function() {
  # For total gaseous carbon losses it seems like CO2 and CH4 need to be handled seperately due to
  # differences in mol. weight
  # I have summed theses elsewhere
  if (restore_hab_in[1] == 2 & restore_hydr_in[1] == 2) {

  }
}
